/**
 * Fleetillo Application Configuration
 * Centralized configuration for the web launcher.
 */
const AppConfig = {
    // API base URL for RPC calls
    API_BASE_URL: '/api',

    // Debug mode toggle
    DEBUG: false,

    // Application version
    VERSION: '1.0.0'
};
