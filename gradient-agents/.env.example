# ============================================================================
# Gradient Agents Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env files with real credentials to version control
#
# These environment variables are used by Gradient ADK agents for Fleetillo.
# Each agent directory may also have its own .env for agent-specific config.
# ============================================================================

# ============================================================================
# REQUIRED: DigitalOcean Configuration
# ============================================================================
# Required for deploying and managing Gradient agents

# DigitalOcean API token
# Get from: DigitalOcean Control Panel > API > Tokens
# Required permissions: Read/Write for Apps, Functions
DIGITALOCEAN_API_TOKEN=your-digitalocean-api-token

# ============================================================================
# REQUIRED: Gradient Model Access
# ============================================================================
# Required for LLM inference calls from agents

# Gradient Model Access Key for serverless inference
# Create at: DigitalOcean Control Panel > Gradient AI Platform >
#            Serverless Inference > Model Access Keys
GRADIENT_MODEL_ACCESS_KEY=your-gradient-model-access-key

# ============================================================================
# REQUIRED: Supabase Configuration
# ============================================================================
# Agents need database access to query Fleetillo data
# Find these values in: Supabase Dashboard > Project Settings > API

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key - used for read operations
# Safe for use in agents as it respects Row Level Security
SUPABASE_ANON_KEY=your-supabase-anon-key

# Database schema (must match main Fleetillo app)
SUPABASE_SCHEMA=fleetillo

# ============================================================================
# Optional: Supabase JWT Secret
# ============================================================================
# Only needed if agents need to verify/create JWTs

# Supabase JWT secret for token verification
# Find in: Supabase Dashboard > Project Settings > API > JWT Settings
# SUPABASE_JWT_SECRET=your-supabase-jwt-secret
